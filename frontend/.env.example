# API base URL (empty string uses same origin — recommended with Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8080

# Temporary: grant all permissions in the UI until GET /api/auth/permissions exists on backend.
# Backend @PreAuthorize remains authoritative. Do not use in production.
VITE_ENABLE_MOCK_PERMISSIONS=false
